// ! normal variable
let a: string | number;

a = 'Shonjoy';

// ! array
let b: (string | number)[] = [];

b.push('Shonjoy', 37);

// ! object
let c: {
  name: string;
  age: number;
  programmer: boolean;
};

c = {
  name: 'Shonjoy',
  age: 21,
  programmer: true,
};
